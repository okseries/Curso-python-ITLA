class Colors:
    # Colores principales
    PRIMARY = "#5C6BC0"      # Azul índigo (para fondos principales o acentos)
    SECONDARY = "#FFCA28"    # Amarillo dorado (para complementar y destacar)

    # Colores de texto
    TEXT_PRIMARY = "#212121" # Gris oscuro (texto principal)
    TEXT_SECONDARY = "#757575" # Gris medio (texto secundario)
    WHITE = "#fff" # Gris medio (texto secundario)

    # Colores de fondo
    BACKGROUND_LIGHT = "#FAFAFA" # Blanco casi puro (fondo general claro)
    BACKGROUND_DARK = "#303F9F"  # Azul profundo (modo oscuro o acentos)

    # Colores de botones
    BUTTON_PRIMARY = "#29B6F6"   # Azul brillante (botones de acción principal)
    BUTTON_SECONDARY = "#FF7043" # Naranja suave (botones secundarios)
    BUTTON_HOVER = "#0288D1"     # Azul más intenso (hover de botones principales)

    # Colores de alerta y estados
    SUCCESS = "#66BB6A"          # Verde suave (éxito)
    WARNING = "#FFB74D"          # Naranja (advertencia)
    ERROR = "#E57373"            # Rojo (error)

    # Colores de bordes o detalles
    BORDER_LIGHT = "#E0E0E0"     # Gris claro (bordes o separadores)
    BORDER_DARK = "#BDBDBD"      # Gris medio (bordes o acentos en modo oscuro)
